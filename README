This is a repo for bigcgi, containing application and server bootstrap code.

See LICENSE

TODO LIST:
- SERVER SETUP
  - service startup (/etc/rc.conf)
  - mongodb config, auth/users
  - smtp config for cork
  - enable suexec (default should be fine)
  - bigcgi user creation, add to sudoers for scripts, change scripts to 700 root:root
  - app virtualenv set up, run
- UNIT TESTS
- ADMIN PANEL
- OTHER CONSIDERATIONS
  - logging, shipping, visualizing (logstash + mongodb)
  - backup server
- ATTACHMENTS
- SECURITY
  - http_proxy vulnerability https://httpoxy.org/
  
MANUAL INSTALL INSTRUCTIONS
  - INSTALL DEPENDENCIES
    - create bigcgi user with adduser
    - install packages sudo, tcl86, python35, apache24, mongodb
    - edit /etc/rc.conf to auto start mongo and apache
    - create an env.sh file and source to shell
    
